# Copie para .env quando precisar apontar para outro endereço.

# Base da API. Em produção, algo como https://api.seudominio.com.br/api
VITE_API_URL=http://localhost:3333/api

# Identificador do estabelecimento no cardápio público (coluna `slug` da tabela empresas).
VITE_EMPRESA_SLUG=bar-do-pardal
