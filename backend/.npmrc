# O build precisa de TypeScript, Prisma CLI e @types/* — todos devDependencies.
# Sem esta linha, qualquer ambiente com NODE_ENV=production (Render, Railway,
# Heroku) instala só as dependências de produção e o `tsc` quebra com dezenas de
# "Could not find a declaration file for module 'express'".
include=dev
